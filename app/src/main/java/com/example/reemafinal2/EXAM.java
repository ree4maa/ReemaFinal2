//package com.example.reemafinal2;

import android.os.Bundle;

import androidx.activity.EdgeToEdge;
import androidx.appcompat.app.AppCompatActivity;
import androidx.core.graphics.Insets;
import androidx.core.view.ViewCompat;
import androidx.core.view.WindowInsetsCompat;

//public class EXAM extends AppCompatActivity {

    //@Override
  //  protected void onCreate(Bundle savedInstanceState) {
      //  super.onCreate(savedInstanceState);
      //  EdgeToEdge.enable(this);
      //  setContentView(R.layout.activity_exam);
       // ViewCompat.setOnApplyWindowInsetsListener(findViewById(R.id.main), (v, insets) -> {
          //  Insets systemBars = insets.getInsets(WindowInsetsCompat.Type.systemBars());
          //  v.setPadding(systemBars.left, systemBars.top, systemBars.right, systemBars.bottom);
          //  return insets;
     //   });
  //  }
//}

//. دورة حياة النشاط (Activity Lifecycle)
//هي المراحل التي تمر بها الشاشة من لحظة تشغيلها حتى إغلاقها.
//onCreate: "الولادة". تتنفذ مرة واحدة فقط عند إنشاء الشاشة لتجهيز التصميم والبيانات.
//onPause: "الانتظار المؤقت". تتنفذ عندما تغطي شاشة أخرى جزءاً من شاشتك (مثل مكالمة واردة).
//onStop: "التوقف". تتنفذ عندما تختفي شاشتك تماماً عن الأنظار وتصبح في الخلفية.
//onResume: "الاستئناف". تتنفذ عندما تعود الشاشة لتصبح هي الواجهة النشطة أمام المستخدم.
//onStart(): يتم استدعاؤها عندما يصبح النشاط مرئيًا.
//onDestroy(): يتم استدعاؤها عند تدمير النشاط.
//onRestart(): يتم استدعاؤها عند إعادة تشغيل النشاط بعد توقفه.
//
//2. قواعد البيانات (Databases)
//المكان الذي نحفظ فيه البيانات لتبقى موجودة حتى لو أغلقنا التطبيق أو الهاتف.
//SQL: اللغة العالمية للتحدث مع قواعد البيانات الجدولية (تعتمد على الجداول، الصفوف، والأعمدة).
//Room: مكتبة حديثة من جوجل تسهل التعامل مع قاعدة بيانات SQLite المحلية داخل الهاتف.
//Firebase Realtime Database: قاعدة بيانات سحابية (Cloud) من جوجل، تخزن البيانات على الإنترنت وتحدثها فوراً لدى كل المستخدمين.
//
//3. مكونات مكتبة Room (التخزين المحلي)
//@Entity (الكيان): هو "قالب" البيانات. نحول فيه "الفئة" (Class) في الجافا إلى جدول في قاعدة البيانات.
//صفات الفئة (Fields): هي المتغيرات داخل الكلاس (مثل الاسم والعمر) وتتحول إلى أعمدة في الجدول.
//@Insert: أمر برمجي وظيفته إضافة بيانات جديدة للجدول.
//@Delete / DELETE: أمر وظيفته حذف سجل معين بناءً على شرط (مثل رقم المعرف id).
//
//
//
//
//
//
//4. أدوات الربط والواجهة (UI & Logic)
//Intent (النية): هو "ساعي البريد" في أندرويد. نستخدمه لطلب فعل معين، وأهم وظائفه الانتقال من شاشة إلى أخرى.
//Adapter (المحول): هو "الجسر" أو "الوسيط". يأخذ قائمة البيانات (مثل قائمة مهام) ويحولها إلى شكل مرئي يظهر للمستخدم في القائمة.
//OnClickListener: هو "المراقب". واجهة برمجة تنتظر ضغطة المستخدم على الزر لتقوم بتنفيذ كود معين (معالجة الحدث).
//addValueEventListener: "المصغي الدائم". خاص بـ Firebase، يراقب البيانات في السحاب وأي تغيير يحدث هناك، يقوم بتحديث التطبيق فوراً تلقائياً.
// المنظور (View) - "واجهة المكتب"
//هو كل شيء تلمسه أو تراه بعينك على الشاشة.
//ببساطة: هو "الديكور" والأثاث. الزر (Button) الذي تضغط عليه، النص الذي تقرأه، والصورة التي تراها.
//مثال: زر "إرسال" في واتساب هو View.
//
//2. الخدمة (Service) - "موظف يعمل في الأرشيف"
//هو مكون يعمل في الخلفية (Background)، أي أنك لا تراه أمامك على الشاشة، لكنه يؤدي عملاً مستمراً.
//ببساطة: هو الموظف الذي يعمل خلف الكواليس. هو المسؤول عن استمرار تشغيل الموسيقى حتى لو خرجت من تطبيق الأغاني وفتحت الفيسبوك.
//مثال: عندما يقوم تطبيق "سبوتيفاي" بتشغيل أغنية وأنت تتصفح تطبيقاً آخر، فهذه Service.
//
//
//3. مزود المحتوى (ContentProvider) - "أمين المكتبة"
//هو المسؤول عن إدارة البيانات ومشاركتها بين التطبيقات المختلفة بشكل آمن.
//ببساطة: إذا أراد تطبيق "إنستجرام" الوصول إلى قائمة الأسماء (Contacts) المخزنة في هاتفك، فإنه لا يدخل مباشرة لقاعدة البيانات، بل يطلب ذلك من "أمين المكتبة" (ContentProvider) الخاص بتطبيق الأسماء.
//مثال: قائمة الأسماء في هاتفك هي أشهر مثال على بيانات يديرها ContentProvider.
//
//4. المستقبل البثي (BroadcastReceiver) - "جهاز اللاسلكي"
//هو مكون وظيفته الاستماع والانتظار لرسائل يرسلها نظام الأندرويد أو تطبيقات أخرى.
//ببساطة: التطبيق يكون "نائماً"، وبمجرد حدوث شيء معين في النظام (مثل: البطارية ضعيفة، الشاحن اتصل، وضع الطيران تفعّل)، يرسل النظام "بثاً" عاماً، فيستيقظ هذا المستقبل للرد على الرسالة.
//مثال: عندما يظهر لك تنبيه بأن "البطارية وصلت إلى 15%"، هناك BroadcastReceiver التقط هذه الإشارة من النظام وأظهر لك التنبيه.
//
//
//
//
//ملخص سريع للفرق بينهم:
//View: تراه وتتفاعل معه.
//Service: يعمل في الخلفية ولا تراه.
//ContentProvider: يوزع البيانات بين التطبيقات.
//BroadcastReceiver: ينتظر إشارات النظام ليتصرف.
//res: مجلد الموارد يحوي ملفات التنسيق وقيم خاصة بالتنسيق وواجهة المستعمل.
//manifest: ملف التعريفات للمشروع (واجهات، خدمات، اذنات استعمال….).
//
//1. دوال التعامل مع السحاب (Firebase)
//هذه الدوال تُستخدم غالباً عند التعامل مع قاعدة بيانات Firebase أو نظام تسجيل الدخول.
//auth (Firebase Auth): كائن يُستخدم للتحكم في عمليات تسجيل الدخول (مثل إنشاء حساب جديد أو تسجيل الدخول).
//ref (DatabaseReference): هو "العنوان" أو "المرجع" الذي يشير إلى مكان محدد في قاعدة البيانات السحابية (مثلاً المرجع الذي يشير لجدول "المستخدمين").
//onSuccess: دالة برمجية (Callback) تتنفذ تلقائياً فقط إذا نجحت العملية التي طلبتها (مثل نجاح رفع صورة أو نجاح تسجيل الدخول).
//onFailure: دالة تتنفذ فقط إذا فشلت العملية (مثل انقطاع الإنترنت أو كلمة مرور خاطئة).
//
//2. دوال وأوامر قواعد البيانات (SQL / Room)
//Query: هو "الطلب". نستخدم هذا المصطلح (أو الـ Annotation في Room) لنكتب خلفه أمر البحث الذي نريده من قاعدة البيانات.
//SELECT *: هو أمر بلغة SQL يعني: "اختر كل شيء". نستخدمه لجلب جميع الأعمدة والبيانات من جدول معين.
//مثال: SELECT * FROM Tasks تجلب كل المهام.
//
//3. دوال التفاعل وواجهة المستخدم (UI & Listeners)
//onClick: هي الدالة التي تحتوي على الكود الذي سيعمل لحظة ضغط المستخدم على الزر.
//LinearLayout: هو نوع من "الحاويات" في التصميم. وظيفته ترتيب العناصر (الأزرار، النصوص) فوق بعضها بشكل رأسي (Vertical) أو بجانب بعضها بشكل أفقي (Horizontal).
//
//4. دوال دورة حياة البيانات (Listeners)
//onDataChange: (موجودة في Firebase) هي دالة "المراقبة". تتنفذ في كل مرة يتم فيها تعديل البيانات في قاعدة البيانات لتحديث الشاشة فوراً.
//ما هو TextInputLayout؟
//هو "حاوية" (Container) ذكية تُحيط بـ TextInputEditText. وظيفتها ليست فقط استقبال النص، بل إضافة لمسات جمالية ووظيفية احترافية.
//
//أهم ميزاته (لماذا نستخدمه؟):
//تسمية متحركة (Floating Label): عندما تضغط داخل المربع للكتابة، يرتفع النص التوضيحي (Hint) للأعلى بشكل جميل بدلاً من أن يختفي.
//إظهار الأخطاء (Error Handling): يمكنك إظهار رسالة خطأ باللون الأحمر تحت المربع مباشرة باستخدام دالة setError().
//عداد الحروف (Character Counter): يمكنه إظهار عدد الحروف التي كتبها المستخدم (مثلاً: 15/100).
//أيقونات مدمجة: مثل أيقونة "العين" لإظهار وإخفاء كلمة المرور تلقائياً، أو أيقونة "حذف النص".
//
//ما هو BottomNavigationView؟
//هو شريط يظهر في أسفل الشاشة، يسمح للمستخدم بالتنقل السريع بين الأقسام الرئيسية للتطبيق (عادةً ما بين 3 إلى 5 أقسام).
//
//ميزاته الأساسية:
//سهولة الوصول: لأنه يقع في أسفل الشاشة، يسهل على المستخدم الوصول إليه بإبهام اليد الواحدة.
//التنقل المباشر: بدلاً من فتح القائمة الجانبية (Drawer)، يمكن للمستخدم الانتقال بضغطة واحدة للقسم الذي يريده.
//الدمج مع المكونات: يعمل بشكل رائع مع الـ Fragments (الأجزاء البرمجية) لتغيير محتوى الشاشة دون الانتقال لـ Activity جديدة.
//
//
//3. البارامتر (MyQuest quest):
//هذا هو الكائن (Object) الذي يحتوي على البيانات التي تريد حفظها. Room ستأخذ القيم الموجودة داخل هذا الكائن وتوزعها على الأعمدة المناسبة في الجدول.
//
//مفتاح رئيسي מפתח ראשי primary key:
//
//هو احد حقول (الاعمدة) لجدول معطيات التي قيمه لا تتكرر بحيت لا توجد قيمتين متساوتين.
//مثال: رقم الهوية، الايميل، رقم سيارة، تلفون، رقم حساب بنك، رقم فيزا، رقم باسبورت
//
//DESC :تنازلي
//ASC :تصاعدي
//
//لماذا نستخدم الـ TAG؟
//الهدف الأساسي من هذه الصفة هو استخدامها مع دالة Log لمراقبة ما يحدث في التطبيق خلف الكواليس.عند فتح نافذة Logcat في أسفل أندرويد ستوديو، يمكنك البحث عن كلمة "LifecycleLab" لترى فقط الرسائل المتعلقة بهذه الشاشة، مما يسهل عليك عملية تصحيح الأخطاء (Debugging).
//
//ما هو الـ ArrayAdapter؟
//هو فئة (Class) جاهزة في أندرويد تُستخدم لربط مجموعة من البيانات (مثل مصفوفة نصوص String[] أو قائمة ArrayList) بـ عنصر عرض واحد (مثل ListView أو Spinner).
//
//كيف يعمل؟
//يقوم الـ ArrayAdapter بتحويل كل عنصر موجود في القائمة إلى كائن مرئي (View) عن طريق استدعاء دالة toString() على العنصر ووضعه داخل TextView.
//
//المكونات الأساسية لبنائه:
//عندما تنشئ ArrayAdapter في الكود، فإنه يحتاج إلى 3 أشياء رئيسية:
//Context (السياق): وعادة ما نكتب this.
//Resource (التصميم): شكل السطر الواحد (مثلاً android.R.layout.simple_list_item_1).
//Objects (البيانات): المصفوفة أو القائمة التي تحتوي على النص
//ما هو addOnCompleteListener؟
//هو مستمع (Listener) يُستخدم مع المهام غير المتزامنة (Asynchronous Tasks) في Firebase. وظيفته أن يخبرك: "لقد انتهيت من العمل، سواء بنجاح أو بفشل".
//
//كيف يعمل؟
//عندما تطلب من Firebase (مثلاً: تسجيل مستخدم جديد أو جلب بيانات)، فإن العملية تستغرق وقتاً لأنها تعتمد على الإنترنت. بدلاً من تجميد التطبيق، يقوم Firebase بتنفيذ المهمة في الخلفية، وعندما تنتهي، يستدعي دالة onComplete.
//
//الفرق بينه وبين onSuccess و onFailure:
//onSuccess: تعمل فقط إذا نجحت العملية.
//onFailure: تعمل فقط إذا فشلت العملية.
//onComplete: تعمل في الحالتين. لذلك، يجب عليك فحص حالة المهمة داخلها باستخدام task.isSuccessful().
//عدم تجميد الواجهة: يضمن بقاء التطبيق مستجيباً أثناء انتظار الرد من الخادم.
//مركزية القرار: يمكنك كتابة كود النجاح والفشل في مكان واحد داخل دالة واحدة.
//إغلاق التنبيهات: مكان مثالي لإخفاء علامة التحميل (ProgressBar) لأن المهمة انتهت بكل الأحوال.
//
//الـ Toast (توست) هو وسيلة بسيطة وأنيقة يستخدمها نظام أندرويد لإظهار رسالة نصية قصيرة للمستخدم في أسفل الشاشة.
//إليك أهم مميزاته وما يجعله فريداً:
//مؤقت وغير تفاعلي: يظهر لبضع ثوانٍ ثم يختفي تلقائياً. لا يمكن للمستخدم الضغط عليه أو التفاعل معه؛ هو فقط لإعطاء معلومة سريعة.
//لا يقطع عمل المستخدم: يظهر فوق واجهة التطبيق الحالية دون أن يغلق الشاشة أو يمنع المستخدم من إكمال ما يفعله.
//سياقي: غالباً ما يُستخدم لتأكيد عملية معينة (مثل: "تم حفظ الملف" أو "تم إرسال الرسالة").
//شرح المكونات:
//context: السياق الحالي (غالباً ما نكتب this إذا كنا داخل Activity).
//النص: الرسالة المراد عرضها بين علامتي تنصيص.
//المدة (Duration): وتأخذ قيمتين محددتين:
//Toast.LENGTH_SHORT: يظهر لمدة ثانيتين تقريباً.
//Toast.LENGTH_LONG: يظهر لمدة 3.5 ثانية تقريباً.
//.show(): أهم جزء! بدونها سيتم بناء التوست في الذاكرة لكنه لن يظهر على الشاشة أبداً.
//
//متى نستخدمه؟
//لإعلام المستخدم بنجاح عملية ما (مثل تسجيل الدخول).
//للتنبيه لوجود خطأ بسيط (مثل "يرجى إدخال كلمة المرور").
//عند الرغبة في إظهار معلومة دون الحاجة لفتح نافذة منبثقة (Dialog) تتطلب ضغط زر "موافق".

//// ربط عنصر الـ BottomNavigationView الموجود في XML بالمتغير البرمجي
//BottomNavigationView bottomNav = findViewById(R.id.bottomNav);
//// عند فتح التطبيق لأول مرة، نعرض الـ HomeFragment بشكل افتراضي
//// beginTransaction() لبدء عملية تغيير (استبدال) Fragment
//// replace() لوضع الـ Fragment داخل الـ FrameLayout
//// commit() لتنفيذ العملية
//getSupportFragmentManager().beginTransaction()
//        .replace(R.id.frameLayout, new HomeFragment())
//        .commit();
//// إضافة مستمع (Listener) للنقر على عناصر الـ Bottom Navigation
//bottomNav.setOnItemSelectedListener(item -> {
//    // تعريف متغير من نوع Fragment لتحديد أي Fragment سيتم عرضه
//    Fragment selectedFragment = null;
//    // فحص أي عنصر تم الضغط عليه حسب الـ ID الخاص به
//    if (item.getItemId() == R.id.home) {
//        // إذا تم الضغط على Home → إنشاء HomeFragment
//        selectedFragment = new HomeFragment();
//    } else if (item.getItemId() == R.id.profile) {
//        // إذا تم الضغط على Profile → إنشاء ProfileFragment
//        selectedFragment = new ProfileFragment();
//    } else if (item.getItemId() == R.id.settings) {
//        // إذا تم الضغط على Settings → إنشاء SettingsFragment
//        selectedFragment = new SettingsFragment();
//    }
//
//    // استبدال الـ Fragment الحالي بالـ Fragment الذي تم اختياره
//    getSupportFragmentManager().beginTransaction()
//            .replace(R.id.frameLayout, selectedFragment)
//            .commit();
//
//    // إرجاع true لإخبار النظام أنه تم التعامل مع الحدث بنجاح
//    return true;
//});